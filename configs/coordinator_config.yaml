coordinator:
  port: 8000
  host: "0.0.0.0"

trust:
  ema_alpha: 0.9
  quarantine_threshold: 0.4
  suspicious_threshold: 0.7
  normalization_ceiling: 100.0   

detection:
  warmup_samples: 20
  alert_z_score: 3.0             
  window_size: 50